# Driver signing private keys and pfx (secrets)
driver-signing/certs/*.key
driver-signing/certs/*.pfx
driver-signing/out/viogpudo-original.sys
driver-signing/out/viogpudo-unsigned.cat

# EfiGuard upstream zip extract (regenerable)
efiguard/EfiGuard-v1.4/

# Generated PPM dumps / screenshots
*.ppm
*.png
*.dmp

# guest-stealth self-contained drop built by package.sh (regenerable; contains
# a copy of apply-gpu-spoof.ps1 — keep single source of truth in deploy/scripts/)
guest-stealth/dist/
